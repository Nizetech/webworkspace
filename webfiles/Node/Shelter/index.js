const blue = require('./blue')
const janet = require('./janet')
const saddie = require('./sadie')

const allCats = [blue, saddie, janet];
console.log(allCats);

module.exports = allCats;

// To install npm type in terminal: npm install<pakage name>